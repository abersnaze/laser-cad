const Draggable = () => {
    React.useEffect(() => {
        if (process.browser) {
            document.addEventListener()
        }
        if (typeof document !== 'undefined') {}
    });
    return <></>;
};

export default Draggable;